spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: false

app:
  admin-bootstrap:
    enabled: true

logging:
  level:
    com.bytebank: INFO
    org.hibernate.SQL: WARN
    org.testcontainers: WARN
